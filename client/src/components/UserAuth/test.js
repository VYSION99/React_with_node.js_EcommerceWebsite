const ApparelData = Object.entries(Apparel).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const GardenData = Object.entries(Garden).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const BeautyData = Object.entries(Beauty).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const SportData = Object.entries(Sport).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const IndustrialData = Object.entries(Industrial).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const ConsumerData = Object.entries(consumer).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const TravelsData = Object.entries(Travels).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const FarmData = Object.entries(farm).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
const FoodpackagesData = Object.entries(FoooPackeages).map(([key, value]) => {
  return {
    value: key,
    label: value,
  };
});
